# Lichess opening explorer now requires authentication.
# Create a personal API token (no scopes needed) at:
#   https://lichess.org/account/oauth/token/create
# Then copy this file to `.env.local`, paste your token below, and restart `npm run dev`.
# `.env.local` is gitignored — never commit your token.
VITE_LICHESS_TOKEN=
