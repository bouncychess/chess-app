import type { ArticleData } from '../types';

const article: ArticleData = {
    title: 'Head size closely linked to Intelligence',
    sections: [
        { text: 'A recent study in rats has confirmed the long held notion that head size has a direct causal relationship with intelligence.' },
    ],
};

export default article;
