import type { ArticleData } from '../types';

const article: ArticleData = {
    title: 'A Bloated Mess',
    sections: [
        { text: 'The latest season of the clive show takes a turn from green screen improv and live feats to mostly seated activities.' },
    ],
};

export default article;
