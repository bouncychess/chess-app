import type { ArticleData } from '../types';

const article: ArticleData = {
    title: 'Tactical Patterns Every Player Should Know',
    sections: [
        { text: 'Forks, pins, skewers, and discovered attacks are the building blocks of chess tactics. Recognize these patterns to win material.' },
    ],
};

export default article;
